# =============================================================================
# Configuration de l'application en production
# Copiez ce fichier en .env et modifiez les valeurs
# =============================================================================

# Application
# Pour CJD80: APP_NAME=komuno, DOMAIN=cjd80.fr
# Pour REP: APP_NAME=rep, DOMAIN=repicardie.fr
APP_NAME=app
DOMAIN=example.com
NODE_ENV=production

# Base de données PostgreSQL
DATABASE_URL=postgresql://appuser:CHANGE_ME@postgres:5432/app_db
DB_USER=appuser
DB_PASSWORD=CHANGE_ME
DB_NAME=app_db

# Redis
REDIS_URL=redis://redis:6379

# MinIO (Object Storage)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY_ID=minioadmin
S3_SECRET_ACCESS_KEY=CHANGE_ME
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_ME

# Session
SESSION_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL

# URLs publiques (adapter selon le domaine)
# CJD80: https://cjd80.fr | REP: https://repicardie.fr
NEXT_PUBLIC_BACKEND_URL=https://example.com
NEXT_PUBLIC_MINIO_ENDPOINT=minio.example.com

# Email (optionnel)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com

# Monitoring (optionnel)
LOKI_HOST=
LOG_LEVEL=info
